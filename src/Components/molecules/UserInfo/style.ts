import styled from "styled-components";

const $UserInfo = styled.div`
    display:flex;
    align-items:center;

    img {
        width:1.25rem;
        height:1.25rem;
        border-radius:1rem;
    }
`;

export default $UserInfo;