import styled from "styled-components";

export const $UserInfo = styled.div`
    display:flex;
    align-items:center;

    img {
        width:1.25rem;
        height:1.25rem;
        border-radius:1rem;
    }
`;